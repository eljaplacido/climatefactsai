# E2E Pre-Launch Audit — Master Ledger (2026-07-01)

45 findings from a 14-lane workflow audit on live prod evidence (API + Cloud SQL +
Cloud Run logs). Adversarial verify pass was cut for cost → evidence is
high-plausibility as-reported by lanes, not independently re-confirmed.

## 🔴 Launch-blockers (fix before publishing)
- **ML-01** FTS split-brain — searching `climate`/`wildfire` matches **0 of ~2,062** articles across ALL search paths (stored `search_tsv` stemmed as Finnish via `language_code='fi'` vs query config `'simple'`). Core evidence retrieval is dead platform-wide.
- **ML-02** bge-m3 semantic retrieval dead — query embeds hit a dead trycloudflare tunnel; backfill worker down since 2026-06-05 (52% on-topic unembedded). Hybrid RAG silently = broken FTS, while methodology asserts `bge-m3` ran.
- **ML-03** Google cookie-consent walls ingested as article bodies (1,298 served), enriched + embedded + given claim verdicts. No ingestion quality gate. 135 new/7d.
- **ML-04** Deep-search emits confident, citation-free (hallucinated) answers; zero-evidence path skips the hallucination check; methodology contradicts the answer.
- **ML-05** Per-article Transparency Report fabricates provenance (names Claude 3.5/DeepSeek; real model = qwen2.5:7b; lists 5 evidence sources never consulted; calls a weighted sum a "Bayesian posterior"). Real 13,506-row provenance exists but isn't linked.
- **ML-16** Paying "Professional" (`pro`) customers collapse to anonymous zero quotas (Stripe webhook writes `pro`; QuotaService normalizes unknown `pro`→`anonymous`). Blast radius unverified (DB unreadable).
- **ML-08** URL-analysis unreachable for free/anon (legacy middleware 429s at 0) while `/api/quota` advertises limit 1 — entitlement honesty break.

## P0/P1 items
| ID | P | Dimension | Title → Fix |
|---|---|---|---|
| ML-01 | P0 | Retrieval | FTS config split-brain. FIX: query `'english'` (verified 2062 hits) at ~8 sites (routes_main:200, routes_query:184, search_routes:116/261/307, chat_routes:575/581/895, deep_search_service:825/833, saved_query_routes:286); robust: fix `language_code` + rebuild `search_tsv`; regression test. |
| ML-02 | P0 | Retrieval | bge-m3 dead. FIX: stable embed host (in-proc/sidecar/hosted API), backfill 1,825 vectors, backlog alert, only report semantic when it contributed. |
| ML-03 | P0 | Ingestion | consent-wall bodies. FIX: resolve google-news redirector→publisher; interstitial/boilerplate rejecter (≥250 real chars); backfill 1,433 rows off-topic + null embeds + reverse 786 claim sets. |
| ML-04 | P0 | Analysis | deep-search hallucination. FIX: on zero-evidence refuse factual claims OR run grounding check on the answer; distinguish "call failed" vs "no results". |
| ML-05 | P0 | Transparency | fabricated provenance. FIX: drive transparency block from real `claim_provenance`; "not run" where absent; real formula not "Bayesian"; link `/api/methodology/audit-trail/article/{id}`. |
| ML-06 | P1 | Transparency | reliability breakdown reconciles with neither score nor formula. FIX: compute displayed breakdown from the SAME components/weights; drop orphaned CARF when 0 claims. |
| ML-07 | P1 | Chat | chat confabulates score basis (Climate Risk described as hazard/sensitivity composite). FIX: inject machine-generated per-score methodology digest; instruct state-only-documented-basis; eval. |
| ML-08 | P1 | Tier/quota | url-analysis dual-gate. FIX: remove url_analysis from legacy RateLimitMiddleware (rate_limiter:408-429) so QuotaService is sole truth; test quota==enforced. |
| ML-09 | P1 | Analysis | url-analysis HIGH/70 not verification-backed, contradicts its fact-checks. FIX: floor to LOW/UNVERIFIED when no supporting verdict; add `score_basis` field. |
| ML-10 | P1 | Analysis | url-analysis verdicts not auditable (evidence chain discarded; evidence:[]). FIX: persist evidence_chain + decomposed_confidence + top evidence; surface drilldown. |
| ML-11 | P1 | Analysis | deep-search explainability payload stripped by response_model. FIX: add sentence_grounding/confidence_envelope/structured_synthesis to DeepSearchResponse; contract test. |
| ML-12 | P1 | Source rating | 62% sources "unknown"/contradictory despite evidence-backed tiers; evidence_url hidden. FIX: bridge source_credibility_tiers→profile labels + re-run mig 070; expose evidence_url + "Why this tier?". |
| ML-13 | P1 | Data sources | CAT scraper stores WRONG rating band for most of 29 + invented 0-100 score; poisons sustainability composite. FIX: pin `ratings-matrix__overall`, kill longest-match fallback, re-sync, persist raw text + confidence. |
| ML-14 | P1 | Data sources | NDC layer = 0 NDC data (UNFCCC emits 0) yet claims ~190 countries. FIX: paginate Climate Watch + full ISO3→ISO2 map + re-sync; until then relabel honestly. |
| ML-15 | P1 | Map | Map "Ask" returns 0 for any topic (golden "Drought in East Africa" broken; tags 100% empty). FIX: make topic clause additive/optional; fall back to corpus retrieval. |
| ML-16 | P1 | Tier/quota | pro→anonymous quota collapse. FIX: add `'pro'` alias to QUOTA_LIMITS_BY_TIER mirroring `professional`; one canonical tier enum. |
| ML-17 | P1 | Reliability | API OOM on 1Gi → 503s. FIX: 2Gi, concurrency 80→~10-16, startup-cpu-boost, drop unused torch/transformers/pandas. |

## P2/P3 items (condensed)
ML-18 ingestion has no quality gate (196 dead-link, 782 thin, 1,349 dup served) → Pydantic gate + quarantine. ML-19 claims pipeline stuck (2,827 pending, 1,479 served no verdict) → sweeper + serving gate. ML-20 country/lang corruption (google-news gl= as country_code; language_code='fi' 100%) → XX for search feeds + langdetect. ML-21 disputed/controversy signals always 0.0 (case mismatch) → lowercase-normalize. ML-22 Temp-Anomaly = weather noise (instantaneous vs partial-month-last-year) → real 1991-2020 climatology. ML-23 adaptation-gap MAX(value) across 24y not latest → DISTINCT ON latest year. ML-24 overall_credibility two vocabularies (lower vs UPPER) → normalize. ML-25 map legends misdescribe fill; Climate-Risk unreachable Severe band, 92% green → align legend/render + recalibrate. ML-26 RU (highest warming) + Fiji hidden (SKIP_COUNTRIES); XX in aggregates unmappable → fix antimeridian/centroid + one XX policy. ML-27 map + chat keyboard/SR inaccessible → focusable alternatives + dialog semantics + aria-live. ML-28 dark mode article cards light-on-light → dark: variants + globals net. ML-29 source vocab split-brain (43/109) + phantom slug domains + unvetted defaults-as-vetted → normalize + canonicalize + gate by article count. ML-30 8 divergent tier tables (anon bypasses deep-research gate, explain-connection unmetered, compare never consumed) → one canonical entitlements table. ML-31 sync DB+LLM block event loop; Anthropic no timeout; Redis unreachable (rate limits fail-open) → to_thread + timeouts + Memorystore. ML-32 enrichment as request-scoped BackgroundTasks on throttled CPU (lost) → move to worker or cpu-throttling=false. ML-33 contracts enforced nowhere (63% routes no response_model, no mypy/ruff CI, hand-rolled FE types) → response_model + openapi codegen + one import root + CI. ML-34 agentic action prompt drops param enums + no validation → include enums + validate. ML-35 article.tags 100% empty → populate at enrichment or repoint to content_category. ML-36 (P3) 38 zero-claim HIGH/MED/LOW verdicts no caveat. ML-37 (P3) Climate-Risk warming→score transform undisclosed. ML-38 (P3) compare 0-bars for absent categories; news-events "controversy"=volume. ML-39 (P3) dead pinned prompt template + stale skill metadata. ML-40 (P3) /map two chat widgets collide + ~11 dead chips; chat can't surface saves/tours/6-of-10 layers. ML-41 (P3) perf tail (pool 20<<80, Perplexity dead, 60s embed, stale-chunk 404s, ML-dep bloat). ML-42 (P3) latent code bugs (dead celery config, None-unsafe .group(), pydantic v1 Field(env=), 294 ruff errors). ML-43 (P3) systemic WCAG gaps (broken skip link, dup <main>, sub-AA contrast, no aria-live/reduced-motion). ML-44 (P3) docs/memory drift (stale SoT, ~5 competing authoritative docs). ML-45 (P3) golden-examples registry empty + url-analysis failure copy → 403 dead-end.

## Completeness gaps (NOT covered — no false all-clear)
Verify pass cut (evidence not re-confirmed); NO security lane (authn/authz/SSRF/injection/CSRF/CVEs — prior 2026-06-26 audit stale); tier blast-radius unquantified (DB tier dist unreadable); billing E2E (proration/refunds/webhook-replay) not audited; a11y was static-source only (no live DOM/contrast/AT/mobile); i18n/translation not audited; company/B3 correctness not validated; Perplexity external layer dead (untestable); no load test; no regression-coverage assessment; GDPR export/deletion + legal-doc currency + EU AI Act Art.50 machine-readable disclosure not covered; cron/worker uptime alerting unknown; cross-surface score consistency only spot-checked.
DISPROVEN (excluded): "corpus systemically HIGH with 0 claims"; "Climate Risk is claim-based" (it IS warming-based → real gap is undisclosed transform ML-37).

## 🏁 FINAL CAPSTONE (after ALL fixes land + final simulation pass)
Regenerate **https://climatefacts.ai/methodology** to describe the TRUE post-fix engine, not aspiration — it is the transparency contract, so it must match reality exactly. Reconcile at minimum: retrieval = English FTS with semantic/bge-m3 **deferred** (do not claim hybrid/semantic runs; ML-01/02); article credibility + Transparency Report driven by **real `claim_provenance`** (model qwen2.5:7b-instruct, honest weighted-sum not "Bayesian"; ML-05/06); Climate Risk = IPCC AR6 SSP2-4.5/2050 warming with the **disclosed** 1.5°C→0 / 5.0°C→10 transform (ML-37); source ratings **derived from the platform's own reliability tiering, not third-party audits** (ML-12/29); Temperature Anomaly framed as provisional year-over-year (ML-22); NDC/CAT provenance corrected (ML-13/14); tier entitlements match what is enforced (ML-08/16/30); the honest live composite (~3.0, backend-driven) already shipped. Verify every claim on the page against a live endpoint/DB fact before publishing.
